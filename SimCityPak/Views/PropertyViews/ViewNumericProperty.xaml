<UserControl x:Class="SimCityPak.ViewNumericProperty"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="38" d:DesignWidth="278" DataContextChanged="UserControl_DataContextChanged">
    <Grid>
        <TextBox Name="txtFloat" Text="{Binding Value, Mode=OneWay}" KeyUp="txtFloat_KeyUp" >
            <TextBox.Style>
            <Style TargetType="{x:Type TextBox}">
                <Setter Property="Background"      Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
            </Style>
            </TextBox.Style>
        </TextBox>
    </Grid>
</UserControl>
